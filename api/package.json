{
  "name": "agentation-feedback-api",
  "version": "1.0.0",
  "description": "SWA managed function for Agentation feedback proxy",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "start": "func start",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
